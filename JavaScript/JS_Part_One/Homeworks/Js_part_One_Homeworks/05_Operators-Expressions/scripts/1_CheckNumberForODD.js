/*
 *   1.  Write an expression that checks if given integer is odd or even.
 */
var log = jsConsole.writeLine;
var number = 49;

if (number % 2 === 0) {
    log(number + " is EVEN");
} else {
    log(number + " is ODD");
}